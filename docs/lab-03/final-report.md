# Lab 3 Final Report - Local Evidence Draft

Status: Local Unit 5 integration evidence is verified. The GitHub PR 5 review,
merge, Issue #40 closure, and separate release PR remain pending.

## Answer Part 1 - Scope and objectives

Lab 3 integrates the completed authentication, Requester, IT Staff, and
Administrator workflows; verifies them with API, UI, and browser checks;
records responsive visual evidence; and prepares a reviewed release from
`lab3-staging` to `main`.

The Unit 5 implementation is on `feature/lab3-integration-evidence` at commit
`4d4f114ab4224844584f59296e368de58d073fb9`. Lab 2 remains a separate completed
baseline and was not modified in this worktree.

## Answer Part 2 - Architecture and data model

The server remains the TypeScript/Express/Prisma service backed by PostgreSQL.
The client remains the React/Vite application. Lab 3 adds the root Playwright
entry point while keeping server and client unit tests in their existing
package boundaries.

The Docker PostgreSQL environment was available for the final verification.
Migration `20260919000000_lab3_auth_requester` was deployed successfully,
`prisma migrate status` reported an up-to-date schema, and the seed was run
twice successfully with stable 10-user/3-ticket seed results. After the final
browser run, a read-only count reported 11 users, 16 tickets, 4 public
comments, and 3 internal notes; the additional records are from the integrated
E2E flow.

## Answer Part 3 - API, ownership, and validation

The server Lab 3 suite passed 22 tests across seven files, covering CSRF-safe
authentication, password-change gating, authorization, migration regression,
staff queue/detail operations, comments and notes, and Administrator user
management. The safety rules include separate public/internal content, role
restrictions, and protection against self-deactivation or removal of the last
active Administrator.

The client suite passed 11 tests across five files. The browser suite passed
the integrated Requester, IT Staff, and Administrator flows, including the
Requester create/comment/resolution path and the staff/admin role boundaries.

## Answer Part 4 - UI, responsive behavior, and accessibility

The Unit 5 E2E definitions cover the Requester shell and regression flow, IT
Staff Queue and Ticket Detail, and Administrator User Management. The shared
fixture captures each scenario at 1280 x 900, 900 x 900, and 390 x 844.

All 12 PNGs were generated by the passing browser run and visually inspected.
They are readable at all three sizes, with no visible clipped controls. The
staff detail captures visibly keep Public Comments and Internal Notes separate.
The visual record does not claim a full keyboard or automated overflow audit;
those items remain partially evidenced.

## Answer Part 5 - Acceptance-criterion traceability

| Acceptance criteria | Coverage | Current status |
|---|---|---|
| AC-01 to AC-05 | Auth API/UI tests and `authentication.spec.ts` | Passed - API, client, and browser checks passed. |
| AC-06 to AC-10 | Requester API/UI tests and `authentication.spec.ts` | Passed - creation, detail, comment, and resolution flow passed. |
| AC-11 to AC-17 | Staff API/UI tests and `staff-ticket-flow.spec.ts` | Passed - queue, detail, ownership, public comments, and internal notes passed. |
| AC-18 to AC-25 | Administrator API/UI tests and `user-administration.spec.ts` | Passed - list, search, create, edit, reset, and role restriction passed. |
| AC-26 | Migration regression, Prisma validation, migration deploy/status, and seed checks | Passed. |
| AC-27 | Responsive captures, visual review, style, and accessibility evidence | Partially evidenced - all required captures are readable, but no dedicated keyboard/overflow audit was run. |

The detailed test matrix remains in [`docs/lab-03/tests.md`](tests.md), and
the figure links and captions are in
[`docs/lab-03/screenshot-index.md`](screenshot-index.md).

## Answer Part 6 - Executed tests, builds, and database checks

| Command | Result |
|---|---|
| `npm test -- --run tests/lab-03` from `server` | Passed - 7 files, 22 tests |
| `npm run build` from `server` | Passed |
| `DATABASE_URL=<local Docker URL> npx prisma validate` | Passed |
| `DATABASE_URL=<local Docker URL> npx prisma migrate deploy` | Passed - Lab 3 migration applied |
| `DATABASE_URL=<local Docker URL> npx prisma migrate status` | Passed - schema up to date |
| `DATABASE_URL=<local Docker URL> npm run prisma:seed` twice | Passed - 10 users and 3 tickets on both runs |
| `npm test -- --run tests/lab-03` from `client` | Passed - 5 files, 11 tests |
| `npm run build` from `client` | Passed - Vite transformed 31 modules |
| `npx playwright test e2e/lab-03` from repository root | Passed - 9 tests, 1 worker, 15.0s |
| `git diff --check` | Passed - exit code 0; normal LF/CRLF warnings only |

The supporting command output and evidence labels are retained in
[`docs/lab-03/release-evidence.md`](release-evidence.md).

## Answer Part 7 - Screenshot index and visual audit

The required twelve-image index is in
[`docs/lab-03/screenshot-index.md`](screenshot-index.md). Figures 1 through 12
are real PNG files generated by the passing Playwright run and were inspected
directly. The updated PDF was rendered page by page; figure numbering and the
real GitHub links were checked.

## Answer Part 8 - Peer review, merge, and issue tracking

The completed feature units are recorded by the following real GitHub links:

- [Lab 3 Issue 1 (#36)](https://github.com/book6349/toktickit/issues/36) and [PR 1 (#41)](https://github.com/book6349/toktickit/pull/41)
- [Lab 3 Issue 2 (#37)](https://github.com/book6349/toktickit/issues/37) and [PR 2 (#42)](https://github.com/book6349/toktickit/pull/42)
- [Lab 3 Issue 3 (#38)](https://github.com/book6349/toktickit/issues/38) and [PR 3 (#43)](https://github.com/book6349/toktickit/pull/43)
- [Lab 3 Issue 4 (#39)](https://github.com/book6349/toktickit/issues/39) and [PR 4 (#44)](https://github.com/book6349/toktickit/pull/44)
- [Lab 3 Issue 5 (#40)](https://github.com/book6349/toktickit/issues/40) is the approved Unit 5 issue.

PR 1 through PR 4 were reviewed and merged into `lab3-staging`. PR 5 has not
been opened, so there is no PR 5 number or link to invent. Its required
Development-panel link, reviewer request, author reply, reviewer approval,
merge, Issue #40 closure, and the separate reviewed `lab3-staging` to `main`
release PR remain pending real GitHub events.

## Answer Part 9 - Limitations and next actions

The remaining work is the controlled GitHub workflow, not unverified local
implementation:

1. Review the local evidence and keep the approved PR 5 body aligned with the
   actual files.
2. Push `feature/lab3-integration-evidence` only when authorized.
3. Open exactly PR 5 to `lab3-staging`, link Issue #40 through Development,
   request the designated review, and reply to every review comment.
4. Wait for the friend approval; the reviewer, not the author, merges PR 5.
5. Update the board and close Issue #40 only when the workflow permits it.
6. Prepare and review the separate release PR from `lab3-staging` to `main`.
