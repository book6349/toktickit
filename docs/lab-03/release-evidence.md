# Lab 3 Unit 5 - Integration Evidence

Status: Partially evidenced. This is the local evidence record for the Unit 5
feature branch. PR 5 and the later release PR have not been opened in this
record.

## Verification snapshot

- Date: 2026-09-19 (Asia/Bangkok)
- Branch: `feature/lab3-integration-evidence`
- Implementation commit: `70cc7de8dca6509639f0bc9176801433fbcca9ec`
- Validation was run after that commit with repository worktree access.
- The Lab 2 worktree and Lab 2 report were not modified.

## Command record

| Area | Exact command | Result | Observed output |
|---|---|---|---|
| Server Lab 3 tests | `cd server` then `npm test -- --run tests/lab-03` | Passed | 7 files passed, 22 tests passed; Vitest duration 4.61s. |
| Server build | `cd server` then `npm run build` | Passed | TypeScript build exited 0. |
| Client Lab 3 tests | `cd client` then `npm test -- --run tests/lab-03` | Passed | 5 files passed, 11 tests passed; Vitest duration 4.02s. |
| Client build | `cd client` then `npm run build` | Passed | TypeScript and Vite production build exited 0; 31 modules transformed. |
| Prisma schema | `cd server` then `npx prisma validate` | Failed - prerequisite unavailable | Prisma P1012: `DATABASE_URL` was not set. |
| PostgreSQL migration and seed | Documented in `docs/lab-03/tests.md`, not run | Deferred | No configured `DATABASE_URL` or disposable PostgreSQL service was available. |
| Integrated Playwright E2E | `npx --no-install playwright test e2e/lab-03` | Unavailable | `npx` cancelled because `playwright@1.63.0` was not installed. |
| Responsive screenshots | Generated by the E2E specs | Unavailable | Browser/database prerequisites were unavailable, so no PNG is claimed. |
| Working report PDF | ReportLab generator, `pdftoppm` render, and structural link check | Passed - draft only | `output/pdf/Lab3_Final_Report.pdf` rendered to 10 pages; all pages were visually inspected, with nine required Answer Part headings and nine real GitHub links verified. |
| Git diff check | `git diff --check` | Passed | Exit code 0. |

The root `package.json` now declares `@playwright/test` and the root
`playwright.config.ts` defines the required command, but dependencies and the
browser binary still need to be installed before E2E execution can be
claimed.

## Evidence labels

- Passed means the exact command ran successfully at the implementation SHA.
- Failed means the exact command ran and returned an error; the error is
  retained above rather than being hidden.
- Unavailable means the required environment or runner was absent.
- Deferred means the check requires a database/service step that has not been
  performed.
- No screenshot, E2E result, migration result, or release event is marked
  Passed without a real captured result.
- The PDF row is only a working-draft artifact check. It is not final visual
  acceptance because the required application screenshots are unavailable.

## Unit 5 implementation record

- Added the root Playwright configuration and test script.
- Added shared seeded-user credentials and responsive capture helpers.
- Added authentication and Requester regression coverage.
- Added IT Staff Queue and Ticket Detail coverage.
- Added Administrator User Management coverage.
- Kept the feature branch separate from the completed Lab 2 worktree.

## Remaining verification before PR 5 approval

1. Install the declared Playwright package and Chromium browser.
2. Start the documented PostgreSQL environment and set `DATABASE_URL`.
3. Run the migration, seed, server, client, and Playwright commands again.
4. Capture and inspect the twelve required responsive screenshots.
5. Update this record with the real integrated branch/SHA and actual results.
6. Only then use the copy-ready PR 5 body and request the designated review.
